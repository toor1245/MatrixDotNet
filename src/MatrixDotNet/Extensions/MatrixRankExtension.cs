using System;

namespace MatrixDotNet.Extensions
{
    public static partial class MatrixExtension
    {
        /*internal static int Rank<T>(this Matrix<T> matrix) where T : unmanaged
        {
            for (int i = 0; i < Math.Min(matrix.Rows,matrix.Columns); i++)
            {
                
            }
        }
        */
    }
}